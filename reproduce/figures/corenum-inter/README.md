# corenum-inter — inter-host scalability (Tier 3)

Reproduces paper Fig 4-right. Tier 3 only. Driver script is a
placeholder until the legacy `pot_eval_inter_host_corenum_*` driver
binaries are surfaced; the figure currently skips with a sentinel.
