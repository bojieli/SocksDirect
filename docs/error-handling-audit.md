# Error-handling audit (Phase 3)

Phase 3 of `REWRITE_PLAN.md` mandates that **user-input paths return
errno-style failures**, not crash the process. `FATAL(...)` is reserved
for detected internal corruption; `assert(...)` is for invariants that
external input cannot violate; `abort()` should be exceptional.

This document enumerates every site in the legacy tree (`common/`,
`lib/`, `monitor/`) where the prototype currently exits or asserts on
something the user can drive into. Each entry needs a disposition:

- **CONVERT** → swap to `errno = X; return -1;` (or equivalent).
- **KEEP** → genuinely internal-only invariant.
- **MOVE** → asserts that should become `static_assert`.

The table below is auto-generated by `tools/scan_error_handling.py`
on every PR; the disposition column lives outside the marker and is
maintained by hand as fixes land.

## Disposition history

| Date | Site | Disposition | Phase 3 PR |
|---|---|---|---|
| _(none yet)_ | | | |

<!-- AUTO-AUDIT-BEGIN -->

_Auto-generated from `tools/scan_error_handling.py`. Re-run on every PR that touches `lib/`, `monitor/`, or `common/`._

**Total sites:** 115 across 20 file(s).

| Kind | Count |
|---|---|
| `FATAL(...)` | 105 |
| `assert(...)` | 10 |

### common/interprocess_t.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 177 | `FATAL` | `FATAL("Failed to open the shared memory, errno: %d", errno);` |
| 181 | `FATAL` | `FATAL("Failed to attach the shared memory, err: %s", strerror(errno));` |
| 234 | `FATAL` | `FATAL("Error to init mutex");` |
| 284 | `FATAL` | `FATAL("Error to init mutex");` |

### common/interprocess_t_n.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 180 | `FATAL` | `FATAL("Unknown command for rst offset, give me some hint"` |
| 239 | `FATAL` | `FATAL("Unknown command for rst offset, give me some hint"` |
| 267 | `FATAL` | `FATAL("unexpected pop_data from ZEROCOPY_RETURN command");` |
| 278 | `FATAL` | `FATAL("unexpected pop_data from ZEROCOPY command");` |
| 285 | `FATAL` | `FATAL("Unknown command in pop_data");` |
| 380 | `FATAL` | `FATAL("Unsupported FD type from 8 byte to 16 byte");` |
| 417 | `FATAL` | `FATAL("Unknown command for rst offset, give me some hint"` |
| 490 | `FATAL` | `FATAL("length mismatch for DATA_TRANSFER_ZEROCOPY_VECTOR");` |
| 516 | `FATAL` | `FATAL("Unsupported FD type from 8 byte to 16 byte");` |

### common/locklessq_v2.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 119 | `assert` | `assert((sizeof(element_t)==16));` |

### common/locklessq_v2_rdma.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 125 | `assert` | `assert((sizeof(element_t)==16));` |

### common/locklessq_v3.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 73 | `assert` | `assert((sizeof(element_t) == 16));` |

### common/locklessqueue_n.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 70 | `assert` | `assert((sizeof(element_t)==16));` |

### common/metaqueue.h

| Line | Kind | Excerpt |
|---|---|---|
| 186 | `FATAL` | `FATAL("Err to malloc");` |
| 194 | `FATAL` | `FATAL("Pop long msg failed, invalid type.");` |

### common/rdma.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 47 | `FATAL` | `FATAL("RDMA config failed to open");` |
| 61 | `FATAL` | `FATAL("Fail to query device %d", dev_i);` |
| 70 | `FATAL` | `FATAL("Failed to query port %d on dev %d", port_i, dev_i);` |
| 79 | `FATAL` | `FATAL(` |
| 92 | `assert` | `assert(ret == 0);` |
| 99 | `FATAL` | `FATAL("Failed to close dev %d", dev_i);` |
| 104 | `assert` | `assert(p->ib_ctx == nullptr);` |
| 124 | `FATAL` | `FATAL("PD nullptr");` |
| 126 | `FATAL` | `FATAL("RECV CQ nullptr");` |
| 130 | `FATAL` | `FATAL("Failed to create QP, %s", strerror(errno));` |
| 142 | `FATAL` | `FATAL("Failed to set QP to init");` |
| 175 | `FATAL` | `FATAL("Failed to modify QP from INIT to RTR");` |
| 195 | `FATAL` | `FATAL("Failed to modify QP from RTR to RTS");` |
| 212 | `FATAL` | `FATAL("Poll CQ Err %s", strerror(ret));` |
| 243 | `FATAL` | `FATAL("wrong ret %d", ret);` |
| 285 | `FATAL` | `FATAL("wrong ret %d", ret);` |
| 314 | `FATAL` | `FATAL("Poll CQ Err %s", strerror(cq_ret));` |
| 344 | `FATAL` | `FATAL("Poll CQ Err %s", strerror(cq_ret));` |

### common/rdma_interprocess_t.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 136 | `FATAL` | `FATAL("Failed to open the shared memory, errno: %d", errno);` |
| 140 | `FATAL` | `FATAL("Failed to attach the shared memory, err: %s", strerror(errno));` |
| 191 | `FATAL` | `FATAL("Error to init mutex");` |

### common/rdma_locklessqueue_n.hpp

| Line | Kind | Excerpt |
|---|---|---|
| 49 | `assert` | `assert((sizeof(element_t)==16));` |

### lib/lib.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 70 | `FATAL` | `FATAL("Failed to open the shared memory, errno: %d", errno);` |
| 73 | `FATAL` | `FATAL("Failed to attach the shared memory, err: %s", strerror(errno));` |
| 145 | `FATAL` | `FATAL("malloc() failed");` |
| 323 | `FATAL` | `FATAL("RDMA Fork prepare fail, %s %d, %d", strerror(errno), errno, ret);` |

### lib/pot_socket_lib.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 659 | `FATAL` | `if (sockfd < FD_DELIMITER) FATAL("Invalid fd");` |

### lib/rdma_lib.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 47 | `FATAL` | `FATAL("RDMA Fork prepare fail, %s %d, %d", strerror(errno), errno, ret);` |
| 59 | `FATAL` | `FATAL("Failed to create Protected Domain for RDMA");` |
| 72 | `FATAL` | `FATAL("Failed to create a large buffer %s", strerror(errno));` |
| 81 | `FATAL` | `FATAL("Failed to reg MR for RDMA");` |
| 89 | `FATAL` | `FATAL("Failed to create a shared CQ");` |
| 101 | `FATAL` | `FATAL("CQ creation failed error %d:", errno);` |
| 140 | `FATAL` | `FATAL("Failted to create client socket, %s", strerror(errno));` |
| 148 | `FATAL` | `FATAL("Failed to create a TCP connection to remote monitor, %s", strerror(errno));` |
| 216 | `FATAL` | `FATAL("Failed to send RDMA QP ACK to peer :%s", strerror(errno));` |
| 224 | `FATAL` | `FATAL("Failed to get RDMA QP ACK from peer :%s", strerror(errno));` |

### lib/setup_sock_lib.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 19 | `FATAL` | `FATAL("Failted to create client setup socket, %s", strerror(errno));` |
| 25 | `FATAL` | `FATAL("Failed to open the connection to local monitor (check if the monitor is running): %s", strerror(errno));` |

### lib/socket_lib.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 87 | `assert` | `assert(virtual_fd >= 0);` |
| 88 | `assert` | `assert(real_fd >= 0);` |
| 155 | `assert` | `assert(virtual_fd >= 0);` |
| 280 | `FATAL` | `FATAL("Fail to find the the receiver who send the takeover req");` |
| 564 | `FATAL` | `FATAL("Dynamic allocation not implemented!");` |
| 580 | `FATAL` | `FATAL("Dynamic allocation not implemented!");` |
| 602 | `FATAL` | `FATAL("could not create virtual fd");` |
| 619 | `FATAL` | `FATAL("pthread_key not initialized");` |
| 647 | `FATAL` | `FATAL("RDMA Fork prepare fail, %s %d, %d", strerror(errno), errno, ret);` |
| 805 | `FATAL` | `FATAL("Only support TCP connection");` |
| 824 | `FATAL` | `FATAL("Invalid remote addr");` |
| 858 | `FATAL` | `FATAL("Failed to get thread specific data.");` |
| 892 | `FATAL` | `FATAL("Invalid RDMA info");` |
| 1037 | `FATAL` | `FATAL("unordered accept response");` |
| 1045 | `FATAL` | `FATAL("Incorrect accept order for different ports");` |
| 1077 | `FATAL` | `FATAL("Invalid RDMA info");` |
| 1103 | `FATAL` | `FATAL("ACK Failed");` |
| 1584 | `FATAL` | `FATAL("zero copy receive return vector: malloc failed %d pages", num_pages);` |
| 1588 | `FATAL` | `FATAL("zero copy received metadata corrupted, ret %d bytes, expected %d pages", buffer_ret, num_pages);` |
| 1773 | `FATAL` | `FATAL("zero copy received metadata corrupted, ret %d bytes, expected %d pages", buffer_ret, 1);` |
| 1777 | `FATAL` | `FATAL("zero copy failed to get return pages");` |
| 1796 | `FATAL` | `FATAL("zero copy receive: malloc failed %d pages", num_pages);` |
| 1800 | `FATAL` | `FATAL("zero copy received metadata corrupted, ret %d bytes, expected %d pages", buffer_ret, num_pages);` |
| 1806 | `FATAL` | `FATAL("zero copy failed to get return pages");` |
| 1839 | `FATAL` | `FATAL("Not implemented yet");` |
| 1842 | `FATAL` | `FATAL("Unknown command %d", ele.command);` |

### monitor/main.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 48 | `FATAL` | `FATAL("Implementation error on counter %ld", counter);` |

### monitor/process.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 39 | `FATAL` | `FATAL("Failed to create shared memory token file %s, errno: %d (%s)", SHM_NAME, errno, strerror(errno));` |
| 43 | `FATAL` | `FATAL("Failed to get the key of shared memory, errno: %d (%s)", errno, strerror(errno));` |
| 46 | `FATAL` | `FATAL("Failed to open the shared memory, errno: %d (%s)", errno, strerror(errno));` |
| 50 | `FATAL` | `FATAL("Failed to attach the shared memory, errno: %d (%s)", errno, strerror(errno));` |
| 195 | `FATAL` | `FATAL("Takeover message cannot forward since key %u not find", shmem_key);` |
| 215 | `FATAL` | `FATAL("Invalid subcommand");` |
| 222 | `FATAL` | `FATAL("Failed to find the shm key");` |
| 231 | `FATAL` | `FATAL("Failed to find peer qid");` |

### monitor/rdma_monitor.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 29 | `FATAL` | `FATAL("Failed to init RDMA setup sock, %s", strerror(errno));` |
| 36 | `FATAL` | `FATAL("setsockopt fail for RDMA listen socket");` |
| 39 | `FATAL` | `FATAL("Failed to bind RDMA setup sock, %s", strerror(errno));` |
| 41 | `FATAL` | `FATAL("Failed to listen on RDMA setup sock, %s", strerror(errno));` |
| 61 | `FATAL` | `FATAL("Failed to create Protected Domain for RDMA");` |
| 66 | `FATAL` | `FATAL("Failed to create a large buffer");` |
| 74 | `FATAL` | `FATAL("Failed to reg MR for RDMA");` |
| 80 | `FATAL` | `FATAL("Failed to create a shared CQ");` |
| 116 | `FATAL` | `FATAL("RDMA accept remote process failed with err: %s", strerror(errno));` |
| 134 | `FATAL` | `FATAL("Failed to create send CQ.");` |
| 197 | `FATAL` | `FATAL("Failed to send RDMA QP ACK to peer :%s", strerror(errno));` |
| 205 | `FATAL` | `FATAL("Failed to get RDMA QP ACK from peer :%s", strerror(errno));` |
| 225 | `FATAL` | `FATAL("Failed to find the shm key");` |
| 234 | `FATAL` | `FATAL("Failed to find peer qid");` |

### monitor/setup_sock_monitor.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 21 | `FATAL` | `FATAL("Cannot create socket for monitor, err: %s", strerror(errno));` |
| 28 | `FATAL` | `FATAL("setsockopt fail for monitor listen socket");` |
| 31 | `FATAL` | `FATAL("Failed to bind for monitor, err: %s", strerror(errno));` |
| 33 | `FATAL` | `FATAL("Failed to listen for monitor, err: %s", strerror(errno));` |
| 44 | `FATAL` | `FATAL("Failed to accept a new attach request");` |
| 61 | `FATAL` | `FATAL("Fail to send ack, %s", strerror(errno));` |

### monitor/sock_monitor.cpp

| Line | Kind | Excerpt |
|---|---|---|
| 46 | `FATAL` | `FATAL("Failed to open the shared memory, errno: %s", strerror(errno));` |
| 49 | `FATAL` | `FATAL("Failed to attach the shared memory, err: %s", strerror(errno));` |

<!-- AUTO-AUDIT-END -->
